# mobile (Flutter)

Run:
```bash
cd /home/danielg/clawd/actual-native/apps/mobile
flutter run
```

This app currently checks `/health` on the Actual server.
Update the base URL in the UI if your server IP changes.
